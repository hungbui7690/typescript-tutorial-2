/*
  Setup
  - npx create vite@latest playground
  - package.json
    "scripts": {
      "build": "<tsc> && vite build",
    },


-------------------------

  - "lib": ["ES2020", "DOM", "DOM.Iterable"],
    -> built in types that are available in our project
  - "strict": true,



*/

export default function App() {
  return <h1>Let's get started!</h1>
}
